[project]
name = "faq-chatbot"
version = "0.1.0"
description = "Professional FAQ chatbot with semantic retrieval, Flask API, and polished web UI."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
    "python-dotenv>=1.2.2",
    "pytest>=8.4.2",
    "pytest-cov>=7.0.0",
    "sentence-transformers>=5.4.1",
]
